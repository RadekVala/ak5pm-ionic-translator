import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cz.utb.fai.translatorapp',
  appName: 'TranslatorApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
